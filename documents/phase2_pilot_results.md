# Phase 2 結果メモ:PINN実装のパイロット学習

対応するコード: `src/pi_fsm/{segments,cache,pinn/models,pinn/loss,pinn/train}.py`, `scripts/phase2_*.py`
計画: `~/.claude/plans/cosmic-giggling-panda.md`(フェーズ2実装計画)

## TL;DR(2026-08-12時点の現状)

**できたこと**
- PINNパイプライン(区間抽出→モデル→物理損失→学習)を実装し、合成データで正しさを確認済み(sanity check PASS)
- 実データでの軌道フィットは良好($\mathcal{L}_{data}\approx0.98$、誤差0.2m程度、区間をt≤2.5秒に絞ることで達成)
- **$F(t),k(t)$の分離が学習ごとにバラバラだった問題(seed間CV 30〜52%)を、滑らかさの正則化(Tikhonov型)で解決。CV<1%まで再現性が改善**。$\beta$を2桁変えても答え($V_{max}\approx3.7$〜$4.5$)は大きく動かず、恣意的な値ではなさそうという状況証拠がある

**未解決の課題**
- 再現性のある値 $V_{max}\approx4.3$ が、フェーズ1のBaseline1($\Delta t=1$s: 12.34)と系統的にズレている(以前のような seed 依存のノイズではなく、安定して低い値)。原因未特定(PINNとBaseline1が厳密には違う対象を見ている可能性/まだ定常値に達していない可能性)
- 現状すべて1試合(J03WPY)のみでの検証。複数試合での確認は未実施

**次にやるべきこと(優先順)**
1. プラトー判定窓を$t>2.5$秒まで広げ、真に定常状態に達しているか確認する(安価、ローカルで可能)
2. 7試合全部のデータで同じ設定を試し、再現性・Baseline1との整合性を確認する(Colab)
3. 複数試合での$\beta$感度の一貫性を確認する

以下、詳細な経緯(時系列の実験記録)。

## 目的

フェーズ2実装計画に従い、①PINNパイプライン(スプリント区間抽出→モデル→損失関数→学習ループ)を実装し、②合成データでの正しさの検証(sanity check)、③実データでの小規模パイロット学習、を行う。

## ①スプリント区間抽出(`segments.py`)

`notebooks/02_segment_exploration.ipynb` で試行錯誤し、以下の設計に落ち着いた:

- 開始条件: $v_0<2.0$ m/s から1秒以内に+2.0 m/s以上増加
- **回転基準は $t{=}0$ の瞬間速度ではなく、最初の1秒間の正味の移動方向**を使う(重要な修正 — 詳細は下記)
- 減速カット: ピークからdecel_margin以上・decel_hold_s以上持続して落ちたら打ち切り。**減速で終わらず10秒上限まで到達した区間は破棄**(ジョグ/位置取りの誤検出とみなす)
- 異常値除外: フレーム間速度が12m/sを超える区間は破棄(トラッキングエラー)
- 最小ピーク速度4m/s未満は破棄

**見つけたバグとその修正**: 当初、回転角を $t{=}0$ の瞬間速度ベクトル($v_x(0),v_y(0)$)から計算していたが、区間は必ず低速(<2m/s)から始まるため、この瞬間速度はTRACABの位置誤差(±1m程度)に対して信号が小さく、方向がほぼランダムになる。その結果、実際には数十メートルに及ぶ横方向の「ドリフト」が回転後の y 座標に現れる区間が多数あった(最大70m超)。**最初の1秒間の正味の移動方向**(ノイズに対して相対的に大きい変位)を回転基準にすることで、y方向の中央値が数十mから約1.9mまで改善した。

J03WPY 1試合: **1332区間**、103,754行、$v_0$ は0〜2.0 m/s(区間開始条件により狭い範囲)。

## ②合成データでのsanity check(`scripts/phase2_sanity_check.py`)

既知の定数 $\alpha_{true}=1.0, V_{max,true}=12.0$ から藤村・杉原モデルの解析解(`baseline.A`, `baseline.B`)で合成軌道を200本生成し($v_0\in[0.2,6.0]$、ノイズ±0.03m)、PINNで $F(t),k(t)$ を復元できるか確認した。

**重要な発見(実装上のバグではなく、モデルの構造的性質)**: $F(t), k(t)$ は $t\to0$ 付近でのみ個別に識別可能で、$t$ が大きくなるとどの $v_0$ から出発した軌道も同じ定常速度 $V_{max}$ に収束するため、$v_0$ 間の「速度のばらつき」という識別に必要な信号が消え、**$F(t)/k(t)=V_{max}$ という比のみ**が識別可能になる。これは物理的に妥当な現象で、むしろフェーズ1で「$\Delta t\lesssim1$秒で不安定性が最も強い」と分かったこととも整合する(興味の中心が同じ早期領域にある)。

この性質を踏まえ、$t\in[0.3, 2.5]$s の早期ウィンドウで評価した結果:

| 指標 | 真値 | 復元値 | 相対誤差 |
|---|---|---|---|
| $F$ | 12.00 | 11.82 | 1.5% |
| $k$ | 1.00 | 0.98 | 2.3% |
| $V_{max}=F/k$(全域中央値) | 12.00 | 12.45 | 3.7% |

**PASS**。実装は正しいと判断し、実データに進んだ。

![sanity check](../outputs/phase2_pinn/sanity_check.png)

## ③実データでのパイロット学習(`scripts/phase2_pilot_train.py`, J03WPY)

Baseline 2($\gamma=0$、物理制約なしの軌道回帰)と提案手法($\gamma=5$)を、1332区間・103,754行で学習(8000エポック、ローカルMPS、あわせて約14分)。

| 指標 | Baseline 2 | 提案手法 |
|---|---|---|
| 最終 $\mathcal{L}_{data}$ | 12.77 | 14.57 |
| 最終 $\mathcal{L}_{physics}$ | — | 0.092 |

参考: Baseline 1($\Delta t=1$s、フェーズ1)は $\alpha=0.95, V_{max}=12.34$。

![pilot結果](../outputs/phase2_pinn/pilot_J03WPY.png)

### 率直な評価:実データでの定量的な復元はまだ信頼できない

$F(t)/k(t)$ の中央値だけを見ると11.45とBaseline1の12.34に近く、一見良い結果に見える。**しかし実際のカーブを見ると、これは見かけ上の一致に過ぎない**:

- $F(t), k(t)$ はどちらも $t\gtrsim1$秒でほぼ0まで潰れている(合成データのときのような、早期ウィンドウでのフラットな復元にはなっていない)
- $F/k$ の比は $t\to0$ 付近で(ほぼ0除算により)15000近くまで跳ね上がってから急落しており、「中央値が近い」のはこの不安定な挙動の集計結果であって、フラットで信頼できる信号ではない

**考えられる原因**(合成データのsanity checkとの違い):
1. **$v_0$ のレンジが狭い**(実データは0〜2m/s、sanity checkは0〜6m/s)。$v_0$ 間のばらつきが $F,k$ を個別に識別する手がかりであるため、レンジが狭いほど識別が難しくなる
2. **$\mathcal{L}_{data}$ が収束しきっていない**(12.8程度で頭打ち、位置誤差にして平均±2.5〜3m相当)。sanity checkの$\mathcal{L}_{data}\approx0.004$と比べて2桁以上大きく、軌道ネットワークが実データの多様性(区間ごとの個体差・ノイズ)を追いきれていない。$(t,v_0)$のみで条件付ける設計(計画の判断3)により、ある程度の残差は想定内だが、現状はその「想定内の残差」よりまだ大きい可能性がある
3. **学習が足りていない可能性**:学習率を緩やかに減衰させ8000エポックまで伸ばしたところ$\mathcal{L}_{data}$は17.4→12.8まで単調に改善し続けており、収束しきる前に打ち切っている

## 追加実験:$v_0$ レンジを広げてみる(仮説の検証)

上記の原因1(「$v_0$ レンジが狭いことが識別性を下げている」)を検証するため、区間開始条件を緩め($v_{low}$: 2.0→6.0 m/s)、$v_0$ を0〜4.95m/s(Baseline1のフィット範囲 $\le6$m/s に近い)まで広げて同条件で再学習した(1597区間、119,741行、6000エポック)。

**結果: 改善しなかった**。$\mathcal{L}_{data}=14.27$(元の14.57とほぼ同じ)。$V_{max}=F/k$ は評価する $t$ の窓によって0.04〜151.6と大きくばらつき、むしろ安定性は悪化した。

これは重要な否定的結果で、**原因1(識別性)よりも原因2(データへのフィット不足そのもの)が主要なボトルネックである可能性が高い**ことを示唆する。$v_0$ の多様性を増やしても $\mathcal{L}_{data}$ が動かなかったということは、軌道ネットワークが実データの形状自体をまだ捉えきれておらず、そこから計算される加速度(延いては $F,k$)が信頼できるレベルに達していない、という段階だと考えられる。次に手を付けるべきは $v_0$ レンジの拡張ではなく、$\mathcal{L}_{data}$ を収束させること(学習量・ネットワーク容量・学習率スケジュール)だと判断する。

## フィット診断実験:なぜ$\mathcal{L}_{data}$が収束しないのか

容量を増やし(隠れ層64→128、深さ4→5)15000エポック学習したところ $\mathcal{L}_{data}$ は7.4まで改善(訓練不足も要因の一つと確認)。個別の軌道フィットを可視化すると、**短い区間(<2秒)はすでに良くフィットしている一方、長い区間(3秒以上)で系統的に外れる**ことが判明(区間長とper-segment MSEの相関係数0.48)。原因は、$(t,v_0)$ という条件付けだけでは「開始速度が同じでも、その後どれだけ加速し続けるか」という個体差を説明できないため。これは学習不足ではなく、条件付け変数が足りないという構造的な限界。

## 追加実験:区間を短く制限する(t≤2.5秒)

上記の診断を踏まえ、全区間を最初の2.5秒に切り詰めて再学習した(1332区間、76,346行、隠れ層128・深さ5、10000エポック)。この2.5秒という範囲は、sanity checkで$F,k$が識別可能だった早期ウィンドウ、かつフェーズ1で不安定性が最も強かった$\Delta t\lesssim1$秒の領域と重なる。

**結果**:
- $\mathcal{L}_{data}=0.98$(中央値0.047、誤差にしておよそ0.2m)— 大幅に改善(直前の実験の7.4、当初の14.6から)
- $F(t), k(t)$ が初めて滑らかな(振動しない)単調減少カーブになった
- $V_{max}(t)=F(t)/k(t)$ も滑らかに減少し、$t\approx1.5$s以降で約29〜30に収束

![短区間実験](../outputs/phase2_pinn/short_segment_experiment.png)

**Baseline1($\Delta t=1$s: $V_{max}=12.34$)とは依然として約2.4倍の差がある。** 解釈は2通りありうる:
1. まだ訓練量・identifiabilityに起因するアーティファクト
2. フェーズ1で確認済みの傾向($\Delta t$を短くするほど$V_{max}$が上昇: $\Delta t=3$s→10.5, $\Delta t=1$s→12.3, $\Delta t=0.2$s→15.6)をそのまま $t\to0$ まで延長した、ある程度妥当な値

現時点ではどちらとも判断できない。1回の学習結果のみでは検証不足であり、複数シード・複数試合での再現性確認が必要。

## Colabでの再現性検証:seedスウィープ

ローカルでのseed比較(seed=0で$V_{max}$プラトー約30、seed=1,2で約40)は、seedとエポック数/warmupが同時に変わっていて交絡していた。Colab(GPU、`notebooks/colab_seed_sweep.ipynb`, `scripts/phase2_seed_sweep.py`)で**スケジュールを完全固定し、seedだけを0〜4の5通り**に振って再検証した(J03WPY、t≤2.5s、隠れ層128・深さ5・10000エポック)。

**結果**: $V_{max}$ プラトー: mean=47.09, std=14.99, **CV=31.8%**。

**判定: まだ不安定(識別性の課題が残る)**。スケジュールを揃えても解消しないことが確定し、「学習不足」ではなく**識別性そのものの問題**であると判断した。

### 追加実験:$v_0$レンジ拡大 × 短区間制限を同時に(Colab)

以前の$v_0$レンジ拡大実験は区間長を制限する前に行っており、$\mathcal{L}_{data}$未収束と交絡していた。今回は短区間制限(既に有効)と組み合わせて再検証(`--v-low 6.0`)。

**結果**: mean=42.31, std=19.21, **CV=45.4%**。**改善どころか悪化**。$v_0$レンジの狭さは主因ではないことが、今度こそクリーンに確認された。

## 重要な発見:なぜ$F,k$が分離できないのか(y方向情報の消失)

$n=(1,0)$ 固定という設計のもとでは、物理損失はx方向とy方向で異なる役割を持つ:

$$\text{x方向}: \quad m\ddot{x} - F(t) + k(t)\dot{x} = 0 \quad(F,k\text{が混在、1本の式では分離不可})$$
$$\text{y方向}: \quad m\ddot{y} + k(t)\dot{y} = 0 \quad(F\text{を含まない。本来はこれだけで}k(t)\text{が単独で決まる})$$

理論上はy方向の式が $k(t)$ を独立に決定し、x方向の式から $F(t)$ が求まるはずだった。しかし「フェーズ1のMSD解析が示す通り実際のスプリントはほぼ直線運動」であるため、y方向の変位は本質的に小さく、TRACABの測定誤差レベルの信号しかない。**$F,k$を分離する唯一の直接的な手がかりが、現象自体の性質(ほぼ直線運動)によって実質的に使えなくなっている**、という構造的な限界である可能性が高い。$v_0$多様性による間接的な分離(x方向の式を複数の$v_0$で連立させる)も、今回の2実験で効果がないことを確認した。

## 結論(2026-08-11時点)

- **パイプライン自体(区間抽出→モデル→損失→学習ループ)は正しく動作することを合成データで確認済み**。フェーズ2最重要ゲートは達成
- **軌道予測($\hat x(t,v_0)$)自体は実データでも良好**($\mathcal{L}_{data}\approx0.98$、誤差0.2m程度)。ただしこれは研究の問い(測定誤差 vs モデルの限界)には答えられない、Baseline 2相当の結果に過ぎない
- **$F(t),k(t)$の個別分離は、現在の設計(nを固定、$(t,v_0)$条件付け、$\hat x$と$F,k$の同時学習)では未解決**。スケジュール・$v_0$レンジという2つの仮説はいずれも否定され、原因はより構造的(y方向情報の消失+同時最適化による共適応)と考えられる
- Baseline1との倍率のズレ(2.4〜3.8倍程度、実行ごとにばらつく)の解釈は依然未決着

## 2段階学習の試行(失敗)

上記の次の一手として2段階学習($\hat x$をデータ損失のみで学習・固定→$F,k$を物理損失のみで学習)を試したが、**悪化した**:

- $\gamma=0$でStage1(完全分離): $V_{max}$プラトー=137.5(大幅悪化)。原因: 物理制約なしで学習した$\hat x$は位置は合っていても加速度(2階微分)が滑らかでなく、Stage2で説明不能な残差を$F,k$が追いかけてしまう
- $\gamma=0.5$でStage1(緩い共適応): 3seedで8.68 / 40.31 / 25.29、**CV≈52%とむしろ悪化**。最初のseed=0の結果(8.68)がBaseline1に近く見えたが、複数seedで見ると「たまたま近かっただけ」と判明

2段階学習というアイデア自体(動くターゲットの切り離し)は妥当だったが、Stage1で物理制約を弱めることで別の問題(微分の滑らかさ喪失)を引き起こし、トータルでは効果がなかった。

## ブレークスルー:$F(t),k(t)$への滑らかさ正則化

識別性の問題は「不良設定な逆問題」(色んな$(F,k)$の組み合わせが同程度にデータに合ってしまう)の典型的症状と判断し、その標準的な対処法である**Tikhonov型の滑らかさ正則化**を実装した(`pinn/loss.py`の`smoothness_loss`、$\beta\int(dF/dt)^2+(dk/dt)^2\,dt$ をコロケーション点で計算し損失に追加)。2段階学習ではなく、**元のシンプルな共同学習に追加**した(メカニズムを混同しないため)。

**結果: 再現性の問題がほぼ完全に解決した。**

$\beta=0.01$、共同学習、J03WPY・t≤2.5s・隠れ層128・深さ5・10000エポックで3seed実行:

| seed | $V_{max}$ プラトー(t>1.5s) |
|---|---|
| 0 | 4.33 |
| 1 | 4.35 |
| 2 | 4.31 |

**CV<1%**。$F(t),k(t)$の値自体もseed間でほぼ完全一致(例: $F$の後半 3.20, 3.20, 3.23)。これまでの30〜52%のばらつきが解消された。

### $\beta$の感度確認

| $\beta$ | $V_{max}$ プラトー | $\mathcal{L}_{data}$ | $\mathcal{L}_{physics}$ |
|---|---|---|---|
| 0.001 | 3.72 | 0.978 | 0.00056 |
| 0.01 | 4.33(3seed一致) | 0.990 | 0.00067 |
| 0.1 | 4.48 | 1.001 | 0.00091 |

$\beta$を2桁変えても$V_{max}$は3.7〜4.5の狭い範囲に収まった。$\mathcal{L}_{data}, \mathcal{L}_{physics}$への悪影響もほぼない。**恣意的なパラメータで答えが動いているのではなく、正則化がある程度の強さを超えると、データが指し示す一つの解に収束している**と解釈できる。

### 残る課題:Baseline1(12.34)とのズレ(ノイズではなく系統的)

再現性は解決したが、$V_{max}\approx4.3$はBaseline1の12.34より低い(以前は逆に高い方向にズレていた)。今回のズレは**seedを変えても動かない系統的なズレ**であり、原因の性質が変わった:

- PINNが見ているのは「低速からの加速開始(t=1.5〜2.5秒時点)」で、Baseline1は全速度域を含む$\Delta t=1$秒の測定 — 厳密には異なる対象を見ている可能性
- この時点でも$F,k$がまだ変化し続けており、真の定常値にはまだ達していない可能性(「プラトー」と呼んでいる区間が本当の収束値でないかもしれない)

未検証・次回以降の課題として残す。

### 次の一手(未着手)

- 7試合全部のデータでの再現性・Baseline1との整合性確認(Colab)
- $t>2.5$秒までプラトー判定窓を広げて、真に定常状態に達しているか確認
- 複数試合での$\beta$感度の一貫性確認

### インフラ

- GitHubリポジトリ: https://github.com/ryu622/pi-fsm (private)
- Colab実行用ノートブック: `notebooks/colab_seed_sweep.ipynb`(Drive マウント→clone→依存インストール→`scripts/phase2_seed_sweep.py`実行、の定型フロー。`--v-low`で区間開始条件を変更可能)
- `pinn/train.py`: `TrainConfig.beta`で正則化強度を指定可能。`train_two_stage`関数も実装済み(現状は非推奨、上記の通り悪化する)
